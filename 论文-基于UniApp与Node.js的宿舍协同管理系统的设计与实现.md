# 河南科技学院

## 2026 届本科毕业论文（设计）

---

### 基于 UniApp 与 Node.js 的宿舍协同管理系统的设计与实现

---

- 学生学号：20221514421
- 学生姓名：杨雅茹
- 所在学院：计算机科学与技术学院
- 所学专业：计算机科学与技术
- 导师姓名：张丽君（教师）
- 完成时间：2026 年 4 月

---

## 摘 要

高校宿舍是大学生日常生活的主要场所，但宿舍内部的值日排班、费用分摊、集体决策等事务长期依赖口头约定或微信群沟通，存在信息容易遗漏、账目不够透明、决策效率偏低等实际困难。

针对上述问题，本论文设计并实现了一套基于 UniApp 与 Node.js 的宿舍协同管理系统，该系统采用前后端分离的 B/S 架构，前端运用 UniApp 框架编写微信小程序，采用 Vue 3 Composition API 组织页面逻辑；后端运用 Node.js 和 Express 框架搭建 RESTful 接口服务，数据存储选用 MongoDB 文档型数据库，借助 Mongoose 完成数据建模。该系统面向高校宿舍全体成员，主要包含宿舍创建与加入、费用分摊记账、投票决策、值日排班、公告发布、消息通知、成员管理等功能。在排班模块中，系统接入了 DeepSeek 大语言模型，用户输入自然语言描述即可自动生成排班方案。系统还引入了基于 WebSocket 的全双工实时通信架构保障消息毫秒级触达，设计了基于动态 CSS 渲染的数据可视化财务大屏，并在排班模块实现了基于大模型与后置规则引擎的混合冲突解决算法，同时支持中、英、韩、日四种语言的界面切换和夜间模式。

经过测试，本系统基本实现各功能模块的预期功能，多用户协同场景下数据同步准确，可以满足宿舍日常事务管理的需要。

**关键词：** 宿舍管理；微信小程序；UniApp；Node.js；协同管理

---

## Abstract

College dormitories are the primary living spaces for university students. However, routine dormitory affairs such as duty scheduling, expense sharing, and group decision-making have long relied on verbal agreements or WeChat group chats, leading to problems like missed information, unclear accounts, and inefficient decision-making.

To address these issues, this paper designs and implements a dormitory collaborative management system based on UniApp and Node.js. The system adopts a B/S architecture with front-end and back-end separation. The front end is built as a WeChat Mini Program using the UniApp framework with Vue 3 Composition API for page logic organization. The back end runs on Node.js with the Express framework, providing RESTful API services, while MongoDB serves as the document-oriented database with Mongoose handling data modeling. The system is designed for all dormitory members, with key features including dormitory creation and joining, expense tracking and splitting, voting, duty scheduling, announcement publishing, message notifications, and member management. The scheduling module integrates the DeepSeek large language model, allowing users to generate scheduling plans through natural language input. The system introduces a full-duplex real-time communication architecture based on WebSocket to ensure millisecond-level message delivery, designs a data visualization financial dashboard based on dynamic CSS rendering, and implements a hybrid conflict resolution algorithm based on LLM and a post-rule engine in the scheduling module. It also supports interface switching among Chinese, English, Korean, and Japanese, along with a dark mode option.

After testing, the system basically realizes the expected functions of each module, and data synchronization remains accurate in multi-user collaborative scenarios, which can meet the needs of daily dormitory management.

**Keywords:** Dormitory management; WeChat Mini Program; UniApp; Node.js; Collaborative management

---

## 目 录

1. 绪论
   1. 研究背景及意义
   2. 国内外研究现状
      1. 国内研究现状
      2. 国外研究现状
2. 系统开发相关技术介绍
   1. 前端技术
   2. 后端技术
   3. 数据库技术
3. 需求分析
   1. 用户需求分析
   2. 用例分析
   3. 交互分析
4. 总体设计
   1. 体系结构设计
   2. 功能模块设计
   3. 数据库设计
5. 详细设计与实现
   1. 公共模块
   2. 宿舍管理模块
   3. 费用管理模块
   4. 投票决策模块
   5. 排班管理模块
   6. 消息通知模块
6. 系统测试
   1. 登录功能测试
   2. 费用管理功能测试
   3. 投票决策功能测试
   4. 排班管理功能测试
   5. 消息通知功能测试
7. 总结与展望
   1. 结论
   2. 展望

- 参考文献
- 致谢

---

## 1 绪论

### 1.1 研究背景及意义

高校宿舍是大学生在校期间主要的生活场所，多名学生共同居住的环境中，值日排班、费用分摊、集体决策等日常事务的管理需求较为普遍。若上述事务处理不当，容易引发成员之间的矛盾，影响宿舍和谐氛围。根据国家统计局 2026 年 2 月发布的《中华人民共和国 2025 年国民经济和社会发展统计公报》，2025 年全国普通、职业本专科在校生达 3954.0 万人，研究生在学人数 430.0 万人，高等教育在学规模持续处于高位，宿舍管理所涉及的学生群体十分庞大。

当前，多数宿舍在处理上述事务时仍采用较为传统的方式。公告信息主要依赖口头传达或微信群消息，信息触达率难以保障；值日排班通常以纸质形式张贴，存在易脱落、易遮挡等问题；费用分摊方面，各成员记录方式不统一，月末对账时常出现数据不一致的情况；集体决策方面，微信群讨论效率较低，消息容易被覆盖，难以形成有效的结论。

近年来，微信小程序在校园场景中的应用日益广泛，其无需单独安装、即用即走的轻量化特性，以及依托微信社交关系链便于分享传播的优势，使其较为适合承载宿舍管理这类使用频率适中但覆盖面广的工具型应用。

基于上述背景，本论文设计并开发了一套宿舍协同管理系统，以微信小程序为载体，将费用记账、投票表决、值日排班、公告通知等常见需求整合至统一平台。在排班模块中，系统创新性地采用了大语言模型与后置规则约束引擎结合的混合排班算法，不仅支持自然语言一键生成排班，更能智能处理排期冲突与数据合并。同时，系统引入了基于 WebSocket 的全双工实时通信技术，打破了传统轮询带来的延迟瓶颈；并针对财务模块设计了轻量级的数据可视化大屏，直观呈现宿舍收支趋势。此外，考虑到部分高校存在留学生与国内学生混住的情况，系统提供了中、英、韩、日四种语言的界面切换功能，并支持夜间模式以适应不同使用场景。从实际运行效果来看，该系统对于降低宿舍内部的沟通成本、提升日常事务管理的透明度与规范性，能够发挥一定的积极作用。

### 1.2 国内外研究现状

#### 1.2.1 国内研究现状

近年来，国内校园信息化建设发展较为迅速，众多高校陆续上线了各类管理系统。然而，现有的宿舍管理系统大多面向宿管人员和辅导员，功能集中在住宿登记、宿舍分配、卫生检查评分等行政管理层面，面向学生的自主使用功能较为有限，更缺乏室友之间的协同管理支持。

在宿舍管理系统方面，陈斐等[1]设计了一款智慧宿舍管理系统，通过数字宿舍地图和室内定位技术实现了查寝、防聚集等管理功能，但其侧重于宿管人员的行政管理需求，未涉及室友之间的费用分摊、集体决策等协同功能。陈柳羽等[2]基于 Java 和 JSP 技术实现了高校数字化宿舍管理系统，推动了宿舍管理的自动化和规范化，但同样未关注宿舍内部成员之间的协同需求。

在前后端分离架构方面，卢洋等[3]探讨了 Web 项目前后端分离的实现方法，该架构思路对本系统的设计具有参考价值。周筱龙等[4]基于 Spring Boot 和 Vue.js 的前后端分离架构对供应商管理系统进行了升级，验证了该架构在提升系统处理效率方面的有效性。刘亚茹等[5]分析了 Vue.js 在前端开发中的优势，尤其是组件化开发和响应式数据绑定两项特性，与本系统前端的技术选型较为契合。

在技术栈方面，项露芬等[6]基于 Vue 和 Node.js 设计了音乐门票管理系统，采用前后端分离架构，前端利用 Vue 框架实现动态交互，后端使用 Node.js 处理数据逻辑，其技术栈与本系统较为接近。黄苗苗[7]同样采用 Vue.js 和 Node.js 实现了作业管理系统，充分利用了前后端分离开发的优势，提升了页面性能和用户体验。孟庆宇[8]在城市轨道交通智能巡检系统中采用 MongoDB 数据库进行数据存储，验证了 MongoDB 在处理多样化数据场景中的适用性。申正阳等[9]研究了基于 JWT 的用户身份和服务权限认证方案，提出了加密存储和 IP 绑定等安全策略，为本系统的身份认证设计提供了参考。

在移动端方面，王婷等[10]设计了一款基于微信小程序的健康管理系统，表明小程序在校园场景中具有无需安装、即开即用的便捷优势。胡军等[11]基于微信小程序开发了高校请假系统，采用前后端分离架构，前端利用 Vue.js 实现动态交互，验证了微信小程序在校园管理场景中的可行性。刘长琦[12]开发了基于微信小程序的智慧党建系统，涵盖党务管理、教育学习等功能模块，进一步表明微信小程序适合承载校园管理类应用。

此外，大语言模型与业务系统的融合是近年来的新兴方向。黄嬿等[13]将 DeepSeek 大语言模型应用于中药饮片处方智能点评系统，通过构建多维知识库和检索增强生成架构，实现了处方的智能审核，审核效率提升显著。该研究表明大语言模型在专业领域的应用具有较大潜力，为本系统在排班模块中集成 DeepSeek 大模型提供了实践参考。

综合来看，现有工具和研究在宿舍内部协同管理这一细分场景上的覆盖尚不充分，费用分摊、集体投票、智能排班等贴近学生日常需求的功能缺少整合性的解决方案。本论文在前人研究的基础上，针对上述具体需求进行了系统的设计与实现。

#### 1.2.2 国外研究现状

国外协同管理工具的发展起步较早，Trello、Notion 等产品在团队协作领域已较为成熟，支持任务分配、进度追踪、投票表决等功能。然而，上述工具面向通用团队设计，未针对高校宿舍场景进行专门适配，且部分高级功能需要付费使用，对学生群体的适用性有限。

在技术层面，王珊等[14]编写的《数据库系统概论》为本系统的数据库设计提供了坚实的理论基础。Cantelon 等[15]分析了 Node.js 的技术特点，指出其事件驱动和非阻塞 I/O 机制在处理并发请求时具有良好的性能表现。Bradshaw 等[16]详细介绍了文档型数据模型的使用方法，并探讨了 MongoDB 在实际场景下的优化策略。Freeman[17]则探讨了 Vue 3 框架中 Composition API 的优势，为前端开发提供了技术指导。上述文献为本系统选择相关技术栈作为开发方案提供了理论支撑。

在跨平台开发框架方面，React Native 和 Flutter 等框架支持开发者编写一套代码同时运行于 iOS 和 Android 平台。UniApp 采用类似的技术路线，但更侧重于国内生态，尤其在微信小程序方面的支持较为完善，且语法基于 Vue.js，前端开发者的学习成本较低。

此外，大语言模型与管理系统的融合是当前值得关注的技术趋势。GPT、DeepSeek 等模型在自然语言理解方面已展现出较强的能力，将其嵌入具体业务系统中，使用户能够通过自然语言完成操作，是当前软件开发领域的热门研究方向。本系统在排班模块中进行了这方面的实践探索。

综合来看，现有工具和研究在宿舍内部协同管理这一细分场景上的覆盖尚不充分，费用分摊、集体投票、智能排班等贴近学生日常需求的功能缺少整合性的解决方案。本论文在前人研究的基础上，针对上述具体需求进行了系统的设计与实现。

---

## 2 系统开发相关技术介绍

### 2.1 前端技术

本系统前端选用 UniApp 框架进行开发。相较于原生微信小程序开发方式，UniApp 采用 Vue.js 语法，开发体验与普通 Web 应用较为接近，且同一套代码可编译为 H5 或 App，具备良好的跨平台扩展能力。本系统当前的编译目标为微信小程序，借助微信自身的社交关系链，室友之间分享邀请卡片较为便捷。

在具体开发中，系统采用 Vue 3 的 Composition API 组织页面逻辑。与 Vue 2 的 Options API 相比，Composition API 支持将相关逻辑集中组织，无需在 data、methods、computed 等选项之间频繁切换，代码的可读性和维护性得到提升。页面中的响应式数据通过 ref 和 reactive 进行声明，需要根据其他数据自动计算的值采用 computed 实现，页面每次显示时需要刷新的数据则在 onShow 生命周期中进行处理。

在网络请求方面，系统基于 uni.request 封装了统一的 request 工具模块，涵盖 GET、POST、PUT、DELETE 四种请求方法。请求发出前自动从本地缓存中获取 JWT 令牌并附加至请求头，响应返回后统一进行错误处理，例如遇到 401 状态码时自动清除登录态并跳转至登录页面。通过该封装，各页面调用接口时仅需编写简洁的调用代码，无需重复处理认证和异常逻辑。

在国际化（i18n）与主题渲染层面，系统摒弃了如 vue-i18n 等体积庞大的第三方库，基于 Vue 3 的响应式系统（Reactivity API）自主研发了一套轻量级的状态机驱动渲染引擎。其核心原理为：通过全局单例的 ref 变量构建状态树，翻译闭包函数 t() 利用依赖收集（Dependency Tracking）机制建立与组件渲染函数的映射关系。切换语言或主题时，系统触发精确到节点级别的局部重绘（Patch），配合 CSS 变量（CSS Variables）实现微秒级的无感暗黑模式切换，在保障多语言（中、英、韩、日）支持的同时，大幅压缩了小程序的包体积（Payload Size），提升了低端设备的渲染帧率。

### 2.2 后端技术

本系统后端采用 Node.js 搭配 Express 框架进行开发。选择 Node.js 的主要考量在于：前端已采用 JavaScript 语言，后端同样使用 JavaScript 可实现前后端数据格式和校验逻辑的共享，有助于提升整体开发效率。Node.js 基于 Chrome V8 引擎运行，采用事件驱动和非阻塞 I/O 的方式处理请求，对于本系统这类以数据增删改查为主的应用而言，性能表现能够满足需求。Express 是 Node.js 生态中应用最为广泛的 Web 框架，在路由定义、中间件挂载、静态文件托管等方面均提供了完善的支持。

后端代码按照职责进行了分层组织。routes 目录存放路由定义，负责将不同的 URL 路径映射至对应的处理函数；controllers 目录存放业务逻辑，每个功能模块对应一个独立文件，如 financeController.js 专门处理费用相关的请求；models 目录存放 Mongoose 的 Schema 定义，规定了各集合的字段名称、数据类型及校验规则；middlewares 目录存放中间件，包括跨域处理、JWT 验证和全局错误捕获等。

身份认证采用 JWT 方案实现。用户通过微信登录后，后端携带前端传入的 code 调用微信 jscode2session 接口获取 openid，随后查询数据库中是否存在对应的用户记录，若不存在则自动创建新用户。之后生成有效期为 7 天的 JWT 令牌返回给前端，前端将其存储至本地缓存，后续每次请求均携带该令牌。后端的 authMiddleware 中间件从请求头中提取令牌进行验证，验证通过后将 userId 挂载至 req 对象，供后续控制器直接使用。

排班模块中还接入了 DeepSeek 大语言模型 API。具体实现方式为：后端首先查询宿舍成员列表，然后将成员名单、日期范围和用户输入的自然语言需求构造为结构化提示词，发送至 DeepSeek 的 chat/completions 接口。AI 返回 JSON 格式的排班数据后，后端对其进行解析，将成员姓名映射为数据库中对应的用户 ID，最终将处理后的排班方案返回给前端展示。

### 2.3 数据库技术

本系统数据库选用 MongoDB，而非传统的关系型数据库 MySQL。做出该选择主要基于实际需求的考量：宿舍管理系统中存在较多数据结构不固定的场景，例如投票的选项数量可能为 2 个至 6 个不等，排班记录的天数每次各异，消息通知的类型也在持续扩展。若采用关系型数据库，上述变化均需修改表结构或建立额外的关联表，灵活性较为不足。MongoDB 作为文档型数据库，数据以类似 JSON 的 BSON 格式存储，单个文档中可直接嵌套数组和子文档，在数据结构的灵活性方面具有明显优势。

系统通过 Mongoose 这一 ODM（对象文档映射）库来操作 MongoDB。Mongoose 支持通过 Schema 为每个集合定义字段类型和校验规则，虽然 MongoDB 本身不强制要求固定的文档结构，但借助 Schema 可使代码中的数据操作更加规范，降低字段名称错误的风险。Mongoose 还提供了 populate 功能，可在查询时自动填充关联文档的内容。例如，查询费用记录时，creatorId 字段存储的是用户 ID，通过 populate 即可直接获取创建者的昵称和头像信息，无需额外执行一次用户查询。

在部署方面，系统采用 MongoDB Atlas 云服务。Atlas 提供免费的共享集群，对于本系统这类用户量有限的项目而言能够满足需求，且自带自动备份和监控面板，免去了自行搭建数据库服务器的工作。不过在校园网环境下连接 Atlas 偶尔会遇到 DNS 解析问题，实际使用时可能需要切换至移动热点或修改系统 DNS 设置。

---

## 3 需求分析

### 3.1 用户需求分析

本系统面向高校宿舍全体成员。在开发之前，通过与本宿舍及周围宿舍的同学交流，了解了学生在日常宿舍生活中遇到的实际困扰。根据学生普遍反馈，主要需求集中在以下几个方面：

**（1）宿舍创建与成员管理需求**

宿舍成员需要一个便捷的方式来建立线上宿舍空间。系统应支持创建宿舍、通过宿舍号加入宿舍、通过微信分享卡片邀请室友加入等功能。同时，宿舍内部需要明确的角色划分，宿舍长应具有一定的管理权限，如发布公告、管理成员等。此外，系统应支持宿舍长选举功能（包括直接指定、随机抽签和匿名投票三种方式），以及成员主动离开宿舍的操作。

**（2）费用分摊与记账需求**

宿舍日常生活中涉及大量的共同开支，如水电费、网费、聚餐费用等。成员普遍反映手工记账容易遗忘且不够透明，希望系统能够提供发起收款、自动计算人均分摊金额、按月统计费用等功能，使宿舍财务管理更加清晰规范。

**（3）集体决策需求**

宿舍内部经常需要进行集体决策，如选择聚餐地点、确定购买物品等。在微信群中讨论往往效率较低，且难以统计每个人的意见。成员希望系统能够提供结构化的投票功能，支持自定义选项、设置截止时间、实时查看投票结果，并支持匿名投票以保护个人隐私。

**（4）值日排班需求**

值日排班是宿舍管理中的常见事务，传统的排班方式通常依赖手工制表或口头约定，容易出现遗忘或不公平的情况。成员希望系统能够自动生成排班表，支持按周或按天轮换，并能够根据特殊情况灵活调整。部分成员还提出了通过自然语言描述排班需求、由系统智能生成排班方案的期望。

**（5）公告通知与消息管理需求**

宿舍内部的重要信息需要及时传达给所有成员。成员希望系统能够提供公告发布功能，支持查看公告的已读状态。同时，系统应具备统一的消息中心，将费用通知、投票提醒、排班提醒、成员变动等各类消息集中展示，支持未读标记、搜索筛选和一键已读等功能。

**（6）个人信息管理需求**

成员希望能够管理自己的个人信息，包括修改昵称、上传头像、填写联系方式和班级信息等，以便室友之间相互了解和联系。

**（7）多语言与个性化需求**

考虑到部分高校存在留学生与国内学生混住的情况，系统应支持多语言切换功能。此外，成员希望系统能够提供夜间模式，以适应不同时段的使用场景，减少夜间使用时对眼睛的刺激。

### 3.2 用例分析

根据需求分析，本系统的用户角色为宿舍成员，其中宿舍长具有额外的管理权限。以下通过用例图展示系统的功能交互关系。

**宿舍成员用例分析**

宿舍成员作为系统的主要用户，可以使用系统提供的全部基础功能。成员用例图如图 3-1 所示。

> 【图 3-1 宿舍成员用例图 —— 请在此处插入用例图】

（1）微信登录：成员通过微信一键授权登录系统，无需额外注册账号，系统自动获取微信 openid 作为用户唯一标识。

（2）个人信息管理：成员可编辑个人昵称、头像、联系方式、班级等信息。

（3）创建/加入宿舍：成员可创建新宿舍或通过宿舍号、分享卡片加入已有宿舍。

（4）待办事项管理：成员可创建、完成和删除个人待办事项，用于记录日常任务。

（5）费用管理：成员可发起收款、查看费用记录、按月统计费用，系统自动计算人均分摊金额。

（6）投票决策：成员可发起投票、参与投票、查看实时投票结果，支持匿名投票和截止时间设置。

（7）排班管理：成员可查看值日排班表，支持手动编辑和 AI 智能生成排班方案。

（8）公告管理：成员可查看宿舍公告，宿舍长可发布和删除公告。

（9）消息中心：成员可查看各类通知消息，支持按类型和状态筛选、关键词搜索、标记已读。

（10）成员管理：成员可查看宿舍成员列表，宿舍长可进行宿舍长选举（支持直接指定、随机抽签、匿名投票三种方式）和成员邀请。

（11）语言切换：成员可在中文、英文、韩文、日文之间切换系统语言。

（12）夜间模式：成员可开启或关闭夜间模式，系统自动调整界面配色。

### 3.3 交互分析

本系统采用前后端分离的 B/S 架构，前端通过 HTTP 请求与后端 RESTful API 进行数据交互。鉴于篇幅有限，以下选取费用管理和投票决策两个核心功能进行交互分析，梳理业务流程并绘制顺序图，以此呈现模块之间的交互细节，为系统的设计与实现提供参考。

**（1）费用管理交互模型**

成员在费用管理页面填写收款标题、金额并选择付款人，点击提交后，前端通过 /api/finance/create 接口将数据发送至后端。后端控制器验证参数完整性和金额有效性，将费用记录存储至 Finance 集合，同时查询宿舍成员列表，向除发起人外的其他成员批量创建消息通知。操作完成后，后端返回成功响应，前端刷新费用列表并显示操作结果。费用管理的顺序图如图 3-2 所示。

> 【图 3-2 费用管理顺序图 —— 请在此处插入顺序图】

**（2）投票决策交互模型**

成员在投票页面填写投票主题、添加选项并设置截止时间，点击发起后，前端通过 /api/decision/create 接口提交数据至后端。后端验证参数有效性（选项不少于 2 个），格式化选项数据后存储至 Decision 集合，同时向宿舍其他成员发送投票通知。成员参与投票时，前端通过 /api/decision/vote 接口提交投票选择，后端检查投票状态、截止时间和是否重复投票，验证通过后更新选项票数和投票记录。投票决策的顺序图如图 3-3 所示。

> 【图 3-3 投票决策顺序图 —— 请在此处插入顺序图】

---

## 4 总体设计

### 4.1 体系结构设计

系统整体采用前后端分离的架构，前端和后端各自独立部署，通过 HTTP 接口进行通信。按照职责划分，系统可分为表现层、业务逻辑层、数据访问层和数据存储层四个层次。体系结构图如图 4-1 所示。

> 【图 4-1 系统体系结构图 —— 请在此处插入体系结构图】

表现层负责用户交互界面的呈现，基于 UniApp 框架开发微信小程序。页面采用 Vue 3 的 Composition API 组织逻辑，布局主要运用 Flex 弹性布局方案，主题切换（日间/夜间模式）通过 CSS 变量实现控制。所有发往后端的请求均自动携带 JWT 令牌，无需各页面单独处理登录态。

业务逻辑层基于 Node.js 运行，采用 Express 框架搭建。该层按功能划分为多个控制器文件，用户相关逻辑由 userController 处理，费用相关逻辑由 financeController 处理，以此类推。各模块之间存在协作关系，例如创建费用记录后，系统会调用消息模块向其他宿舍成员发送通知。跨域处理、JWT 校验、全局错误捕获等通用逻辑统一在中间件中实现。

数据访问层通过 Mongoose 实现。每个业务实体对应一个 Schema 文件，定义字段类型、是否必填、默认值等规则。查询时若需关联其他集合的数据（如查询费用记录时需获取创建者的昵称），可通过 Mongoose 的 populate 方法实现，无需编写多次查询并手动拼接。

数据存储层采用 MongoDB Atlas 云数据库。文档型的存储方式较为适合本系统的数据特点，投票的选项数量不固定、排班的天数各异等场景均可通过文档嵌套数组直接存储，无需像关系型数据库那样建立大量关联表。

### 4.2 功能模块设计

本系统面向宿舍全体成员，共设计 11 个功能模块，包括微信登录、个人信息管理、宿舍管理、待办事项、费用管理、投票决策、排班管理、公告管理、消息通知、成员管理以及系统设置。系统功能模块结构如图 4-2 所示。

> 【图 4-2 系统功能模块结构图 —— 请在此处插入功能模块图】

为了更加清晰地展示系统各模块的内部逻辑与具体能力，下面对每个功能模块进行进一步拆分，并以表格形式详细罗列各模块包含的具体功能。

#### 4.2.1 微信登录模块

微信登录模块旨在提供便捷、安全的身份认证服务，大幅降低用户的接入成本。通过集成微信开放平台的授权接口，用户无需繁琐地注册账号和记忆密码即可快速进入系统。这不仅提升了首次使用的流畅度，也为后续基于微信社交关系链的宿舍邀请等功能奠定了基础。该模块的具体功能如表 4.1 所示。

**表 4.1 微信登录模块功能表**

| 功能名称 | 功能描述                                                                                     |
| -------- | -------------------------------------------------------------------------------------------- |
| 授权登录 | 用户通过微信授权一键登录系统，无需额外注册账号及设置密码。                                   |
| 身份绑定 | 系统自动获取微信 openid，以此作为唯一标识与系统用户进行绑定，并生成 JWT 令牌以维持登录状态。 |
| 资料引导 | 针对首次登录系统的新用户，系统自动识别并跳转至个人信息编辑页面，引导其完善基础资料。         |

#### 4.2.2 个人信息管理模块

个人信息管理模块用于维护用户在系统中的基础资料数据，便于宿舍成员之间的相互识别与日常联系。系统允许用户自定义头像、昵称、班级等个性化标签，既能展现个人特色，也能在多人协同操作（如记账、投票）中提供清晰的身份标识，增强宿舍内部的归属感与交互体验。该模块的具体功能如表 4.2 所示。

**表 4.2 个人信息管理模块功能表**

| 功能名称     | 功能描述                                                                         |
| ------------ | -------------------------------------------------------------------------------- |
| 基础信息编辑 | 支持用户随时修改个人昵称、联系电话、所在班级以及个性签名等基础信息。             |
| 头像上传     | 允许用户通过调用本地相册或相机上传个人头像，文件由服务器端中间件处理并安全存储。 |
| 信息展示     | 在个人主页及宿舍成员列表等场景中，直观展示用户的各项基本信息及自定义头像。       |

#### 4.2.3 宿舍管理模块

宿舍管理模块是整个协同系统的核心基石，负责建立并维护室友之间的数字化空间连接。无论是新生开学时的新建宿舍，还是中途调整时的加入与退出，该模块都提供了闭环的管理流程。通过绑定唯一的宿舍号，系统将原本分散的个体紧密聚合为一个协作整体，从而支撑后续所有公共事务的开展。该模块的具体功能如表 4.3 所示。

**表 4.3 宿舍管理模块功能表**

| 功能名称 | 功能描述                                                                   |
| -------- | -------------------------------------------------------------------------- |
| 创建宿舍 | 用户可创建全新的宿舍空间，系统将自动分配唯一宿舍号，创建者默认成为宿舍长。 |
| 加入宿舍 | 用户可通过输入指定宿舍号或点击微信分享卡片，快速申请并加入已有宿舍。       |
| 离开宿舍 | 支持用户在需要时主动退出当前宿舍，退出后系统自动切断其与该宿舍的数据关联。 |

#### 4.2.4 待办事项模块

待办事项模块主要用于辅助用户记录个人学业或宿舍集体的日常零散任务，提升生活的规划性与条理性。考虑到大学生日常事务繁杂，该模块采用轻量化的交互设计，帮助用户随时随地记录灵感或备忘，并通过首页直观的列表展示，有效避免重要事项的遗漏。该模块的具体功能如表 4.4 所示。

**表 4.4 待办事项模块功能表**

| 功能名称 | 功能描述                                                                 |
| -------- | ------------------------------------------------------------------------ |
| 新增待办 | 支持用户快速输入任务内容，创建新的个人日常待办事项。                     |
| 状态管理 | 提供勾选操作，支持用户将待办事项在“未完成”与“已完成”状态之间灵活切换。   |
| 事项删除 | 对于已完成或不再需要的待办记录，支持用户进行删除清理操作。               |
| 首页展示 | 在系统首页的显著位置直观展示当前未完成的待办事项列表，起到备忘提醒作用。 |

#### 4.2.5 费用管理模块

费用管理模块旨在解决宿舍内部公共开支（如水电费、网费、聚餐等）的记账与分摊难题，实现集体财务的完全透明化。传统的口头记账极易引发记忆偏差和账目纠纷，而该模块通过智能化的自动分摊计算与多维度的数据统计，让每一笔开销都有据可查，从根本上维护了宿舍的和谐关系。该模块的具体功能如表 4.5 所示。

**表 4.5 费用管理模块功能表**

| 功能名称   | 功能描述                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------ |
| 发起收款   | 支持用户填写收款标题、金额、备注，并可通过多选框灵活指定参与本次分摊的宿舍成员。           |
| 自动分摊   | 系统内置高精度计算逻辑，根据指定的付款总人数自动计算并展示人均应摊金额。                   |
| 费用统计   | 提供按月度维度的账单汇总，统计总支出、人均支出及记账笔数，并支持按指定付款人进行筛选查询。 |
| 数据可视化 | 提供轻量级的可视化大屏，直观呈现各项收支占比的饼图以及近几个月费用的趋势柱状图。           |

#### 4.2.6 投票决策模块

投票决策模块用于规范和加速宿舍内部针对公共事务（如聚餐地点、卫生安排、寝室规定等）的意见统一过程。在多人居住环境中，众口难调是常见现象，该模块将非结构化的群聊讨论转化为结构化的投票表决，并支持匿名模式以保护隐私，从而确保每一次集体决策都能高效、民主地得出结论。该模块的具体功能如表 4.6 所示。

**表 4.6 投票决策模块功能表**

| 功能名称 | 功能描述                                                                      |
| -------- | ----------------------------------------------------------------------------- |
| 发起投票 | 支持用户自定义投票主题，灵活设置 2-6 个选项，并可根据需要配置投票的截止时间。 |
| 匿名投票 | 提供匿名投票开关选项，开启后投票过程及结果中将隐藏投票人的真实身份信息。      |
| 结果统计 | 系统实时处理投票请求，动态计算并展示各选项的当前得票数及其占总票数的百分比。  |
| 投票管理 | 投票发起人拥有管理权限，可对尚未结束的投票记录进行编辑修改或彻底删除。        |

#### 4.2.7 排班管理模块

排班管理模块通过数字化的手段，彻底解决了宿舍值日排班易遗忘、分配不均以及临时调班困难等问题。该模块不仅提供了清晰的日历视图供成员随时查看，更创新性地引入了 AI 智能排班算法，能够理解并满足复杂的个性化排期需求，极大地减轻了宿舍长的管理负担。该模块的具体功能如表 4.7 所示。

**表 4.7 排班管理模块功能表**

| 功能名称      | 功能描述                                                                                           |
| ------------- | -------------------------------------------------------------------------------------------------- |
| 日历视图      | 采用日历网格形式，清晰、直观地展示当前月份每日的值日人员安排情况。                                 |
| 手动/批量排班 | 允许用户手动点击日期指定值日人员，同时提供控制栏实现多选日期的批量分配或批量清除。                 |
| AI 智能排班   | 支持用户输入自然语言排班需求，系统结合大语言模型与规则引擎智能生成方案，并自动处理冲突与数据合并。 |
| 历史记录      | 每次排班保存后，系统自动留存历史排班记录，方便成员后续进行追溯和核对。                             |

#### 4.2.8 公告管理模块

公告管理模块作为宿舍内部重要信息的官方发布渠道，能够确保关键信息的高效、准确传达。相较于容易被聊天记录淹没的微信群消息，该模块不仅提供了醒目的展示位置，还配备了已读状态追踪机制，确保每一位成员都能及时获取停水停电、卫生检查等重要资讯。该模块的具体功能如表 4.8 所示。

**表 4.8 公告管理模块功能表**

| 功能名称 | 功能描述                                                                 |
| -------- | ------------------------------------------------------------------------ |
| 发布公告 | 赋予宿舍长管理权限，支持其编写并发布包含清晰标题和正文内容的宿舍公告。   |
| 公告查看 | 宿舍成员可浏览历史公告列表，并点击进入详情页查看完整的公告内容。         |
| 已读追踪 | 系统自动记录并更新每条公告的阅读状态，直观展示已读和未读成员的详细名单。 |
| 删除公告 | 针对过期或失效的公告，宿舍长可进行删除操作，以保持公告列表的整洁。       |

#### 4.2.9 消息通知模块

消息通知模块集中处理系统内的各类异步事件，扮演着信息调度中心的角色。无论是费用的催缴、投票的邀请，还是排班的临近，系统都会依托底层的全双工通信架构，将这些关键动态以毫秒级的速度精准推送到相关用户的终端，确保宿舍协作的无缝衔接。该模块的具体功能如表 4.9 所示。

**表 4.9 消息通知模块功能表**

| 功能名称   | 功能描述                                                                                     |
| ---------- | -------------------------------------------------------------------------------------------- |
| 实时推送   | 依托 WebSocket 全双工通信架构，实现财务、投票、排班等各类系统消息的毫秒级实时推送。          |
| 筛选与搜索 | 支持用户按消息的阅读状态（未读/已读）及具体类型进行筛选，并提供关键词模糊搜索功能。          |
| 状态管理   | 允许用户对单条消息标记已读，或使用一键“全部已读”功能，系统同步更新底部导航栏的未读红点角标。 |

#### 4.2.10 成员管理模块

成员管理模块主要负责维护宿舍内部的人员结构及其相应的权限分配。系统通过清晰的成员列表和角色标识，帮助大家了解当前的宿舍构成；同时，设计了公平、透明的宿舍长选举机制以及便捷的邀请途径，确保了宿舍管理工作的有序交接与新鲜血液的顺利融入。该模块的具体功能如表 4.10 所示。

**表 4.10 成员管理模块功能表**

| 功能名称   | 功能描述                                                                             |
| ---------- | ------------------------------------------------------------------------------------ |
| 成员列表   | 清晰展示当前宿舍内的所有成员信息，并明确标注各成员的角色身份（宿舍长或普通成员）。   |
| 宿舍长选举 | 提供系统化换届机制，支持通过直接指定、随机抽签、匿名投票三种模式公平选举新任宿舍长。 |
| 成员邀请   | 宿舍长及成员可通过生成专属链接、手机号等方式，邀请新室友加入当前宿舍。               |

#### 4.2.11 系统设置模块

系统设置模块提供了全局维度的个性化配置选项，旨在满足不同用户群体的多样化使用偏好。考虑到高校内可能存在的留学生混住情况以及用户夜间使用手机的习惯，该模块集成了多语言即时切换与夜间模式适配功能，从细节处全面提升了系统的可用性与人机交互体验。该模块的具体功能如表 4.11 所示。

**表 4.11 系统设置模块功能表**

| 功能名称 | 功能描述                                                                                      |
| -------- | --------------------------------------------------------------------------------------------- |
| 语言切换 | 系统内置轻量级国际化方案，支持在中文、英文、韩文、日文四种语言之间进行即时无缝切换。          |
| 夜间模式 | 提供深色/浅色主题开关，开启夜间模式后，系统自动调整全局界面的背景及文字配色，以适应暗光环境。 |

### 4.3 数据库设计

#### 4.3.1 数据库概念结构设计

为了清晰展示各核心模块的数据关联关系，本系统将概念结构设计按照业务逻辑拆分为三个局部的 E-R 图进行展示，分别为个人与财务模块、宿舍与排班模块、投票决策模块。

**（1）个人与财务模块 E-R 图**

该模块主要展示用户与他产生的私人数据、财务数据之间的关联。核心逻辑为：用户可以发起多条收款记录（1:N），也可以作为付款人参与多条费用分摊（M:N）；同时用户可以创建多条待办事项，接收多条消息通知。个人与财务模块 E-R 图如图 4-3 所示。

> 【图 4-3 个人与财务模块 E-R 图 —— 请在此处插入你生成的图】

**（2）宿舍与排班模块 E-R 图**

该模块展示宿舍这个核心实体与内部成员、排班计划、公告管理之间的组织架构。核心逻辑为：一个宿舍包含多名成员，可以安排多期排班表，拥有多条公告；而用户作为宿舍成员，可以参与多次值日排班，也可以发布多条公告，同时系统通过已读记录关联表来追踪公告的阅读状态（M:N 拆分）。宿舍与排班模块 E-R 图如图 4-4 所示。

> 【图 4-4 宿舍与排班模块 E-R 图 —— 请在此处插入你生成的图】

**（3）投票决策模块 E-R 图**

投票决策涉及相对复杂的交互逻辑，因此单独展示。核心逻辑为：一个宿舍可以产生多次投票决策；用户作为发起人可以创建多条投票，作为参与者可以对投票中的多个选项进行选择，从而生成投票记录（M:N 拆分）。投票决策模块 E-R 图如图 4-5 所示。

> 【图 4-5 投票决策模块 E-R 图 —— 请在此处插入你生成的图】

#### 4.3.2 数据库逻辑结构设计

由于本系统采用 MongoDB 文档型数据库，数据以 JSON 文档形式存储，以下列出各核心集合的字段设计。

**（1）用户集合（User）**

用户集合存储系统中所有注册用户的信息，通过 openid 字段与微信账号关联，通过 dormId 字段与所属宿舍关联。用户集合的字段设计如表 4.2 所示。

**表 4.2 用户集合字段表**

| 字段      | 类型     | 说明                                    |
| --------- | -------- | --------------------------------------- |
| \_id      | ObjectId | 主键，自动生成                          |
| openid    | String   | 微信 openid，唯一标识                   |
| nickname  | String   | 用户昵称，默认"宿舍成员"                |
| avatar    | String   | 头像地址                                |
| signature | String   | 个性签名                                |
| phone     | String   | 联系电话                                |
| class     | String   | 班级信息                                |
| dormId    | ObjectId | 外键，关联宿舍集合                      |
| role      | String   | 角色（member 普通成员 / leader 宿舍长） |
| createdAt | Date     | 注册时间                                |

**（2）宿舍集合（Dorm）**

宿舍集合存储宿舍的基本信息，包括宿舍号、公告内容和成员列表。宿舍集合的字段设计如表 4.3 所示。

**表 4.3 宿舍集合字段表**

| 字段       | 类型       | 说明                       |
| ---------- | ---------- | -------------------------- |
| \_id       | ObjectId   | 主键，自动生成             |
| dormNumber | String     | 宿舍号（如"5号楼306"）     |
| notice     | String     | 宿舍公告内容               |
| members    | ObjectId[] | 成员 ID 数组，关联用户集合 |
| creator    | ObjectId   | 创建者 ID，关联用户集合    |
| createdAt  | Date       | 创建时间                   |

**（3）财务记录集合（Finance）**

财务记录集合存储宿舍内的收款和分摊信息。字段设计如表 4.4 所示。

**表 4.4 财务记录集合字段表**

| 字段      | 类型       | 说明                                |
| --------- | ---------- | ----------------------------------- |
| \_id      | ObjectId   | 主键，自动生成                      |
| dormId    | ObjectId   | 外键，关联宿舍集合                  |
| title     | String     | 收款标题                            |
| amount    | Number     | 金额（元）                          |
| creatorId | ObjectId   | 发起人 ID，关联用户集合             |
| payeeIds  | ObjectId[] | 付款人 ID 数组                      |
| status    | String     | 状态（unpaid 未收款 / paid 已收款） |
| createdAt | Date       | 创建时间                            |

**（4）投票决策集合（Decision）**

投票决策集合存储宿舍内发起的投票信息，选项和投票记录以嵌套文档形式存储。字段设计如表 4.5 所示。

**表 4.5 投票决策集合字段表**

| 字段      | 类型     | 说明                                               |
| --------- | -------- | -------------------------------------------------- |
| \_id      | ObjectId | 主键，自动生成                                     |
| dormId    | ObjectId | 外键，关联宿舍集合                                 |
| title     | String   | 投票主题                                           |
| options   | Array    | 选项数组，每项含 label（选项文本）和 count（票数） |
| voters    | Array    | 投票记录数组，每项含 userId 和 optionIndex         |
| deadline  | Date     | 截止时间                                           |
| creatorId | ObjectId | 发起人 ID                                          |
| status    | String   | 状态（active 进行中 / ended 已结束）               |
| anonymous | Boolean  | 是否匿名投票                                       |
| createdAt | Date     | 创建时间                                           |

**（5）排班集合（Schedule）**

排班集合存储宿舍的值日排班信息，排班明细以嵌套数组形式存储。字段设计如表 4.6 所示。

**表 4.6 排班集合字段表**

| 字段      | 类型     | 说明                                                     |
| --------- | -------- | -------------------------------------------------------- |
| \_id      | ObjectId | 主键，自动生成                                           |
| dormId    | ObjectId | 外键，关联宿舍集合                                       |
| weekLabel | String   | 排班周期标签（如"2026-04-01 ~ 2026-04-07"）              |
| cycle     | String   | 轮换方式（weekly 按周 / daily 按天）                     |
| items     | Array    | 排班明细数组，每项含 date、weekday、personId、personName |
| createdAt | Date     | 创建时间                                                 |

**（6）消息集合（Message）**

消息集合存储用户收到的各类通知消息。字段设计如表 4.7 所示。

**表 4.7 消息集合字段表**

| 字段       | 类型     | 说明                                       |
| ---------- | -------- | ------------------------------------------ |
| \_id       | ObjectId | 主键，自动生成                             |
| userId     | ObjectId | 外键，关联用户集合                         |
| type       | String   | 消息类型（vote / task / finance / system） |
| content    | String   | 消息内容                                   |
| status     | String   | 状态（unread 未读 / read 已读）            |
| createTime | Date     | 创建时间                                   |

**（7）公告集合（Announce）**

公告集合存储宿舍发布的公告信息，已读用户列表以数组形式存储。字段设计如表 4.8 所示。

**表 4.8 公告集合字段表**

| 字段      | 类型       | 说明                    |
| --------- | ---------- | ----------------------- |
| \_id      | ObjectId   | 主键，自动生成          |
| dormId    | ObjectId   | 外键，关联宿舍集合      |
| title     | String     | 公告标题                |
| content   | String     | 公告内容                |
| creatorId | ObjectId   | 发布者 ID，关联用户集合 |
| readBy    | ObjectId[] | 已读用户 ID 数组        |
| createdAt | Date       | 创建时间                |

---

## 5 详细设计与实现

系统共包含 11 个功能模块，受篇幅限制，以下选取若干核心模块进行详细说明，包括登录和个人信息管理两个公共功能模块，以及费用管理、投票决策、排班管理、消息通知四个业务模块。

### 5.1 公共模块

#### 5.1.1 登录功能

登录功能采用微信一键授权方式实现，用户无需注册账号或填写用户名密码，通过微信授权即可完成登录。

**（1）界面设计**

登录页面采用渐变色背景设计，页面中央展示系统 Logo 和应用名称"DormSync"，下方设有"微信一键登录"按钮。页面整体风格简洁明快，通过 CSS 线性渐变实现从蓝色到浅灰色的过渡效果。夜间模式下，背景色自动切换为深色调，登录按钮配色也相应调整，保证在不同光线环境下的视觉舒适度。登录界面如图 5-1 所示。

> 【图 5-1 登录界面 —— 请在此处插入截图】

**（2）实现过程**

用户点击登录按钮后，前端调用微信 uni.login 接口获取临时登录凭证 code，随后通过 /api/user/login 接口将 code 发送至后端。后端接收到 code 后，携带 AppID 和 AppSecret 调用微信 jscode2session 接口，获取用户的唯一标识 openid。后端根据 openid 查询用户集合，若用户不存在则自动创建新用户记录（默认昵称为"宿舍成员"），随后生成有效期为 7 天的 JWT 令牌，连同用户信息一并返回给前端。前端将令牌和用户信息存储至本地缓存，完成登录流程。

登录成功后，系统根据用户状态进行智能跳转：若用户昵称仍为默认值，则跳转至个人信息编辑页面引导完善资料；若用户尚未加入宿舍，则跳转至宿舍设置页面；若用户已有宿舍，则直接进入系统首页。此外，系统还支持通过微信分享卡片携带宿舍号参数，当用户通过分享卡片进入时，登录后自动跳转至加入宿舍页面并预填宿舍号。登录功能的程序流程图如图 5-2 所示。

> 【图 5-2 登录功能程序流程图 —— 请在此处插入流程图】

#### 5.1.2 个人信息管理

个人信息管理功能允许用户编辑昵称、头像、联系电话、班级和个性签名等信息。首次登录时，系统会自动跳转至该页面，引导用户完善基本资料。

**（1）界面设计**

个人信息编辑页面采用表单布局，顶部展示当前头像（支持点击更换），下方依次排列昵称、联系电话、班级、个性签名等输入项，底部设有保存按钮。头像上传采用微信原生的 chooseImage 接口选择图片，通过 uni.uploadFile 将图片上传至后端服务器的 uploads 目录，后端使用 Multer 中间件处理文件上传并返回可访问的图片 URL。个人信息编辑界面如图 5-3 所示。

> 【图 5-3 个人信息编辑界面 —— 请在此处插入截图】

**（2）实现过程**

用户修改信息后点击保存按钮，前端通过 /api/user/update 接口将更新数据提交至后端。后端控制器从请求中提取用户 ID（优先从 JWT 令牌解析，兼容请求体传入），构建更新对象（仅包含用户实际修改的字段），调用 Mongoose 的 findByIdAndUpdate 方法更新用户文档，并开启 runValidators 选项确保数据符合 Schema 定义的验证规则。更新成功后返回最新的用户信息，前端同步更新本地缓存。

### 5.2 宿舍管理模块

宿舍管理模块是系统的基础模块，提供宿舍的创建、加入、公告管理和成员管理等功能。

#### 5.2.1 宿舍创建与加入功能

**（1）界面设计**

系统首页顶部展示当前宿舍号和欢迎语，下方设有费用管理、投票决策、排班管理、宿舍成员四个快捷入口卡片，采用等宽四列布局。底部展示个人待办事项列表，支持新增和删除操作。页面支持下拉刷新，刷新时同步更新宿舍信息、待办列表和未读消息数。首页界面如图 5-4 所示。

> 【图 5-4 系统首页界面 —— 请在此处插入截图】

**（2）实现过程**

宿舍创建流程：用户在宿舍设置页面输入宿舍号并点击创建按钮，前端通过 /api/dorm/create 接口提交数据。后端首先检查用户是否已加入其他宿舍（防止重复创建），然后验证宿舍号是否已存在（防止重复），验证通过后创建宿舍文档，将创建者添加至成员列表，并将用户角色更新为宿舍长（leader）。

宿舍加入流程：用户输入宿舍号或通过分享卡片获取宿舍号后，前端通过 /api/dorm/join 接口提交加入请求。后端验证用户未加入其他宿舍且目标宿舍存在后，将用户添加至宿舍成员列表，更新用户的宿舍关联信息，并向宿舍现有成员发送新成员加入的通知消息。

#### 5.2.2 宿舍成员管理功能

**（1）界面设计**

宿舍成员管理功能主要由成员列表、信息查看、权限转让与新成员邀请四个部分构成。

首先，成员管理页面以列表形式展示当前宿舍的所有成员，并在醒目位置明确标注每个成员的身份（如“宿舍长”或“成员”）。对于宿舍长角色，页面底部会提供额外的管理入口，而普通成员则仅具备“离开宿舍”的功能。宿舍成员管理列表界面如图 5-5 所示。

> 【图 5-5 宿舍成员管理列表界面 —— 请在此处插入截图】

其次，点击列表中的任意成员，系统会弹出一个详细的信息卡片，展示该成员的头像、昵称、手机号及所在班级等基本信息，方便室友间相互了解与联系。查看成员信息界面如图 5-6 所示。

> 【图 5-6 查看成员信息界面 —— 请在此处插入截图】

在权限管理方面，当宿舍长点击“转让宿舍长”时，系统提供了灵活且公平的选举机制，包括直接指定、随机抽签和匿名投票三种模式，确保权限交接过程的民主性。宿舍长选举界面如图 5-7 所示。

> 【图 5-7 宿舍长选举界面 —— 请在此处插入截图】

最后，对于“邀请新成员”功能，宿舍长可通过输入目标用户的手机号进行精准邀请，或者生成专属的宿舍邀请卡片分享至微信群，新成员点击卡片即可一键加入。邀请新成员界面如图 5-8 所示。

> 【图 5-8 邀请新成员界面 —— 请在此处插入截图】

**（2）实现过程**

宿舍长转让与成员变动：前端通过 /api/dorm/elect 接口实现宿舍长的变更，后端在验证请求者身份后，将目标用户的角色更新为 leader，原宿舍长降级为 member。在成员邀请方面，前端提交手机号至 /api/dorm/invite，后端查询目标用户状态并将其加入当前宿舍。同时，系统在宿舍长变更、新成员加入或成员退出（/api/dorm/leave）等状态发生改变时，会触发异步消息通知机制，确保所有宿舍成员及时掌握人员变动动态。

#### 5.2.3 宿舍公告管理功能

**（1）界面设计**

系统首页中部展示最新宿舍公告。点击可进入公告详情页面。宿舍长具有编辑公告的权限，可以通过输入框更新公告内容。公告编辑与详情展示界面如图 5-9 所示。

> 【图 5-9 宿舍公告管理界面 —— 请在此处插入截图】

**（2）实现过程**

公告的更新由宿舍长通过 /api/dorm/notice 接口完成。后端接收到新的公告内容后，更新 Dorm 集合的 notice 字段。更新成功后，系统会下发全员通知，提醒宿舍其他成员查看最新的公告内容。

### 5.3 费用管理模块

费用管理是宿舍成员反馈需求最为强烈的功能之一。水电费、网费、聚餐分摊等日常共同开支，此前主要依赖微信转账或口头记账，时间较长后容易出现账目不一致的情况。该模块的核心设计思路为：付款人发起一条收款记录，系统根据参与分摊的人数自动计算人均金额，同时向其他宿舍成员发送通知。

#### 5.3.1 收款与费用分摊功能

**（1）界面设计**

费用管理页面提供"发起收款"功能，点击展开收款表单，包含收款标题输入框、金额输入框（限制为数字类型）、参与分摊人员多选区域（以标签形式展示宿舍成员，点击选中或取消，支持选择多人）和备注输入框。人均分摊金额根据实际选中的参与人数自动计算，而非固定按宿舍总人数平分。发起收款界面如图 5-10 所示。

> 【图 5-10 发起收款界面 —— 请在此处插入截图】

**（2）实现过程**

发起收款时，前端对表单数据进行前置校验（标题非空、金额大于零、已选择至少一名参与人），校验通过后通过 /api/finance/create 接口提交数据。后端控制器接收请求后，验证必填参数的完整性和金额的有效性。在分摊计算中，系统引入了高精度浮点数运算机制与余数补偿算法。考虑到分摊金额除不尽引发的精度丢失问题（如 100 元分摊至 3 人），算法会在清算时对尾差进行动态补偿，确保总账目金额绝对守恒。系统创建财务记录文档并存储至 Finance 集合后，采用异步事务处理过滤掉发起人，调用消息模块向其他成员批量创建收款通知消息。

#### 5.3.2 费用明细与财务统计功能

**（1）界面设计**

页面中部设有搜索栏和筛选器，支持按关键词搜索费用记录和按付款人筛选。费用列表按月份分组展示，每条费用记录以卡片形式展示，包含费用图标、标题、发起人、时间、金额和人均分摊金额。此外，页面顶部设有数据可视化大屏（Dashboard），包含上方各项支出占比的动态饼图，以及下方近 6 个月宿舍总开销走势的柱状图或折线图。费用明细与统计界面如图 5-11 所示。

> 【图 5-11 费用明细与统计界面 —— 请在此处插入截图】

**（2）实现过程**

费用列表的展示采用了前端内存计算与动态视图映射的方案。前端从后端获取原始账单（/api/finance/list）后，通过 Vue 3 的 computed 惰性计算机制，在内存中构建虚拟数据视图，实现高精度人均分摊金额的实时运算、多维度动态过滤及月度总额计算。

数据统计大屏的核心数据由后端 /api/finance/statistics 接口提供支持。后端控制器通过 Mongoose 聚合管道（Aggregation Pipeline），利用条件过滤与正则表达式按关键词将开销归类聚合为饼图数据，并按年月对近 6 个月支出记录求和汇总输出趋势图数据。前端利用 CSS conic-gradient 与动态高度演算机制，原生渲染直观的饼图与走势图，提升了可视化加载体验。费用管理的程序流程图如图 5-12 所示。

> 【图 5-12 费用管理程序流程图 —— 请在此处插入流程图】

### 5.4 投票决策模块

宿舍内部经常需要进行集体决策，如选择聚餐地点、确定物品采购方案等。此前在微信群中讨论时，消息量较大容易导致重要信息被覆盖，难以有效统计各成员的意见。投票决策模块旨在将非结构化的讨论转化为结构化的投票，使决策结果清晰直观。

#### 5.4.1 投票发起与设置功能

**（1）界面设计**

投票页面顶部设有"发起投票"按钮，点击展开投票表单，包含投票主题输入框、选项输入区域（支持动态添加和删除选项，最少 2 个最多 6 个）、截止日期和时间选择器。同时，系统提供“匿名投票”的开关。投票列表以卡片形式展示，每张卡片包含投票主题、状态标签（进行中/已结束）、截止时间、选项列表和操作按钮（编辑/删除）。投票发起界面如图 5-13 所示。

> 【图 5-13 投票发起界面 —— 请在此处插入截图】

**（2）实现过程**

发起投票时，前端校验投票主题非空、有效选项不少于 2 个、已设置截止日期后，通过 /api/decision/create 接口提交数据。后端将选项数组统一格式化为包含 label 和 count 字段的对象数组（count 初始值为 0），创建投票文档并存储至 Decision 集合，同时向宿舍其他成员发送投票通知。对于匿名投票模式，后端在持久化存储时利用 UUID 对投票人进行匿名哈希化映射，从协议层保护投票人的隐私隔离。

#### 5.4.2 投票参与与结果展示功能

**（1）界面设计**

对于未投票的用户，选项区域以可点击的按钮形式展示，点击即可完成投票；对于已投票的用户，选项区域以进度条形式展示各选项的得票数和百分比。匿名投票的卡片在列表中左侧带有橙色边框标识。点击投票卡片弹出详情弹窗时，非匿名投票会显示每个选项的投票人列表，而匿名投票则仅展示票数与进度条，隐藏所有参与者的个人信息。投票结果界面如图 5-9 所示。

> 【图 5-9 投票结果界面 —— 请在此处插入截图】

**（2）实现过程**

参与投票时，前端通过 /api/decision/vote 接口提交投票选择（包含投票 ID 和选项索引）。后端执行多重验证：检查投票是否存在、投票状态是否为进行中、是否已超过截止时间、选项索引是否有效。为了防止重复投票，后端会校验 voters 数组，若当前用户 ID 已存在于该数组中，则拒绝该次投票请求。验证通过后，将对应选项的票数加 1，并在 voters 数组中记录用户 ID 和选择的选项索引，最后保存更新后的文档。投票结果的百分比计算在前端通过 getPercent 函数实现，该函数计算各选项票数占总票数的比例并四舍五入取整。投票决策的程序流程图如图 5-10 所示。

> 【图 5-10 投票决策程序流程图 —— 请在此处插入流程图】

### 5.5 排班管理模块

值日排班是宿舍管理中较易引发成员间分歧的事务之一。排班是否公平、是否存在遗漏等问题，依靠口头约定往往难以有效解决。排班管理模块提供了日历视图来展示每日值日安排，支持手动编辑排班，同时集成了 AI 智能排班功能。

#### 5.5.1 手动排班与日历展示功能

**（1）界面设计**

排班页面顶部设有月份导航栏，支持前后切换月份。导航栏下方为星期头行和日历格子区域，日历格子以 7 列 6 行的网格形式展示当月日期，每个格子显示日期数字和值日人员姓名。当日格子以蓝色背景高亮标识，已排班的格子显示对应的值日人员姓名。点击日历格子弹出详情弹窗，可手动指定或清除该日的值日人员。为了提高操作效率，日历视图中还加入了批量操作控制栏，支持多选日期后进行一键批量分配或清除。

**（2）实现过程**

手动排班时，用户点击日历格子选择值日人员，前端在本地更新排班数据。点击保存后，前端通过 /api/schedule/create 接口将排班数据提交至后端，后端通过基于文档型数据库的时间分片聚合算法创建排班文档并存储至 Schedule 集合。保存成功后，系统会执行异步检查，一旦匹配到包含明日的排期，则触发事件驱动机制向对应成员推送异步通知。

#### 5.5.2 AI智能排班功能

**（1）界面设计**

页面下方设有 AI 智能排班区域，包含自然语言输入框和生成按钮，以及三条示例提示（如"按照成员顺序轮流排班""周末不排班，工作日轮流"等），点击示例可自动填充输入框。AI 生成结果会直接回显在日历上，用户可点击具体日期进行微调，确认无误后再点击保存按钮将排班方案持久化。排班管理界面如图 5-11 所示。

> 【图 5-11 排班管理界面 —— 请在此处插入截图】

**（2）实现过程**

用户在输入框输入自然语言需求后，前端将文本状态封装并提交至后端的 /api/schedule/ai-generate 接口。在 Node.js 中间层，系统首先构建了深度系统提示词（System Prompt）以约束大模型的输出边界与注意力权重。随后，系统调用 DeepSeek 的 chat/completions 接口，将其作为自然语言理解引擎。为了解决大模型偶发的“幻觉”现象，系统将 AI 返回的 JSON 方案作为候选解，送入 Node.js 侧的双层校验架构中进行碰撞测试，对排班的公平性、时间冲突性进行二次校验。此外，系统在持久化前置入了一套智能合并策略，能够精准比对现有历史排班与 AI 新生方案，仅对 AI 涉及的日期范围进行覆盖，有效防止了未提及日期的排班数据丢失。排班管理的程序流程图如图 5-12 所示。

> 【图 5-12 排班管理程序流程图 —— 请在此处插入流程图】

### 5.6 消息通知模块

系统各业务模块产生的通知消息（如收款通知、投票提醒、值日提醒、成员变动通知等）统一汇聚至消息中心，使用户无需逐一访问各功能页面即可获知最新动态。

#### 5.6.1 消息列表与分类检索功能

**（1）界面设计**

消息中心页面顶部设有搜索栏和筛选按钮。搜索栏支持按关键词模糊搜索消息内容；筛选按钮点击后从底部弹出筛选面板，支持按消息状态（全部/未读/已读）和消息类型（全部/投票/事务/财务/宿舍/公告）进行组合筛选。消息列表以卡片形式展示，每条消息包含类型图标（不同类型使用不同颜色背景和 Emoji 图标）、类型标签、时间描述（支持"刚刚""X分钟前""X小时前"等相对时间格式）和消息内容。消息中心界面如图 5-13 所示。

> 【图 5-13 消息中心界面 —— 请在此处插入截图】

**（2）实现过程**

消息列表的获取通过 /api/message/list 接口实现，支持按状态和类型进行服务端筛选。用户输入搜索关键词时，前端调用 /api/message/search 接口，后端利用 MongoDB 的正则匹配（$regex）进行模糊搜索，实现对消息内容的快速定位。

#### 5.6.2 消息实时提醒与阅读状态管理功能

**（1）界面设计**

在系统导航栏（TabBar）的消息图标上方，会实时显示未读消息数量角标。消息页面的搜索栏下方设有未读消息提示栏，显示当前未读消息数量和"全部已读"按钮。未读消息在卡片左侧添加蓝色边框标识，右侧显示红色未读圆点。点击消息自动标记为已读，并根据消息类型跳转至对应的功能页面。

**（2）实现过程**

为了保证消息提醒的实时性，系统基于 Socket.io 建立了 WebSocket 全双工实时通信架构。后端的各个业务模块在完成主体逻辑后，会异步调用消息控制器。该模块将消息持久化存储至 Message 集合后，立即通过 io.to(userId).emit() 方法向目标用户的独立房间下发 'new_message' 事件，实现毫秒级主动推送。在阅读状态管理方面，全部标记已读通过 /api/message/read-all 接口实现，后端使用 Mongoose 的 updateMany 批量更新状态；未读消息数量通过 /api/message/unread-count 接口获取，前端在生命周期中调用该接口动态管理角标展示。

---

## 6 系统测试

系统开发完成后进行了功能测试，主要采用黑盒测试方法，重点验证各模块在正常操作和异常输入下的表现。鉴于篇幅限制，以下选取登录、费用管理、投票决策、排班管理和消息通知五个模块的测试情况进行说明。

### 6.1 登录功能测试

登录功能通过微信授权实现一键登录，测试目的在于验证登录流程的正确性、首次登录的引导逻辑以及异常情况的处理能力。测试用例如表 6.1 所示。

**表 6.1 登录功能测试用例表**

| 编号 | 测试子项                 | 预置条件                         | 操作步骤             | 预期结果                               | 执行结果                   | 测试结果 |
| ---- | ------------------------ | -------------------------------- | -------------------- | -------------------------------------- | -------------------------- | -------- |
| 1    | 首次登录                 | 用户未注册，微信授权正常         | 点击"微信一键登录"   | 登录成功，跳转至个人信息编辑页面       | 登录成功，跳转至编辑页     | 正确     |
| 2    | 已注册用户登录（无宿舍） | 用户已注册但未加入宿舍           | 点击"微信一键登录"   | 登录成功，跳转至宿舍设置页面           | 登录成功，跳转至宿舍设置页 | 正确     |
| 3    | 已注册用户登录（有宿舍） | 用户已注册且已加入宿舍           | 点击"微信一键登录"   | 登录成功，跳转至系统首页               | 登录成功，跳转至首页       | 正确     |
| 4    | 通过分享卡片登录         | 用户未加入宿舍，通过分享卡片进入 | 点击分享卡片后登录   | 登录成功，跳转至宿舍设置页并预填宿舍号 | 宿舍号已预填，跳转正确     | 正确     |
| 5    | 令牌过期重新登录         | 用户令牌已过期                   | 访问任意需认证的接口 | 提示"登录已过期"，跳转至登录页面       | 提示正确，跳转正确         | 正确     |

根据以上测试用例，当微信授权正常且用户信息完整时，预期效果为登录成功并根据用户状态跳转至对应页面，实际项目实现效果与预期目标一致。登录成功界面如图 6-1 所示。

> 【图 6-1 登录成功界面 —— 请在此处插入截图】

### 6.2 费用管理功能测试

费用管理功能支持发起收款、查看费用记录和费用统计。测试目的在于验证费用创建的正确性、数据校验的有效性以及统计计算的准确性。测试用例如表 6.2 所示。

**表 6.2 费用管理功能测试用例表**

| 编号 | 测试子项         | 预置条件                           | 输入                                 | 预期结果                                      | 执行结果                       | 测试结果 |
| ---- | ---------------- | ---------------------------------- | ------------------------------------ | --------------------------------------------- | ------------------------------ | -------- |
| 1    | 正常发起收款     | 用户已登录，宿舍有 4 名成员        | 标题：电费，金额：200，选中全部 4 人 | 创建成功，人均显示 50.00 元，其他成员收到通知 | 创建成功，计算正确，通知已发送 | 正确     |
| 2    | 金额为零         | 用户已登录                         | 标题：测试，金额：0                  | 提示"金额必须大于0"，记录未创建               | 提示正确，未创建               | 正确     |
| 3    | 金额为负数       | 用户已登录                         | 标题：测试，金额：-50                | 提示"请输入有效金额"，记录未创建              | 提示正确，未创建               | 正确     |
| 4    | 标题为空         | 用户已登录                         | 标题：空，金额：100                  | 提示"请填写完整信息"，记录未创建              | 提示正确，未创建               | 正确     |
| 5    | 未选择参与人     | 用户已登录                         | 标题：水费，金额：80，未选择任何人   | 提示"请选择付款人"，记录未创建                | 提示正确，未创建               | 正确     |
| 6    | 选择部分成员分摊 | 用户已登录，宿舍有 4 名成员        | 标题：外卖，金额：60，选中 3 人      | 创建成功，人均显示 20.00 元                   | 创建成功，计算正确             | 正确     |
| 7    | 按月统计验证     | 当月已有 3 条费用记录，总计 300 元 | 查看统计卡片                         | 当月总支出 300.00 元，3 笔                    | 统计数据正确                   | 正确     |

根据以上测试用例，当输入的收款标题、金额和参与人信息均符合要求时，预期效果为创建成功并正确计算人均分摊金额，实际项目实现效果与预期目标一致。费用管理成功界面如图 6-2 所示。

> 【图 6-2 费用管理成功界面 —— 请在此处插入截图】

根据以上测试用例，当用户输入的金额为零、负数或标题为空、未选择参与人时，预期目标为提示相应的错误信息且记录未创建，实际项目实现效果与预期目标一致。费用管理异常提示界面如图 6-3 所示。

> 【图 6-3 费用管理异常提示界面 —— 请在此处插入截图】

### 6.3 投票决策功能测试

投票决策功能支持发起投票、参与投票和查看结果。测试目的在于验证投票创建的正确性、防重复投票机制的有效性以及投票结果统计的准确性。测试用例如表 6.3 所示。

**表 6.3 投票决策功能测试用例表**

| 编号 | 测试子项       | 预置条件                 | 输入/操作                                   | 预期结果                             | 执行结果             | 测试结果 |
| ---- | -------------- | ------------------------ | ------------------------------------------- | ------------------------------------ | -------------------- | -------- |
| 1    | 正常发起投票   | 用户已登录，宿舍有成员   | 主题：聚餐地点，选项：食堂/外卖，截止：明天 | 投票创建成功，其他成员收到通知       | 创建成功，通知已发送 | 正确     |
| 2    | 选项不足       | 用户已登录               | 主题：测试，仅 1 个选项                     | 提示"选项至少需要2个"                | 提示正确，未创建     | 正确     |
| 3    | 未设置截止时间 | 用户已登录               | 主题：测试，2 个选项，未选日期              | 提示"请选择截止日期"                 | 提示正确，未创建     | 正确     |
| 4    | 正常投票       | 投票进行中，用户未投票   | 点击选项"食堂"                              | 投票成功，显示投票结果百分比         | 投票成功，百分比正确 | 正确     |
| 5    | 重复投票       | 用户已对该投票投过票     | 再次点击选项                                | 提示"你已经投过票了"                 | 提示正确，票数未变   | 正确     |
| 6    | 投票已结束     | 投票已超过截止时间       | 点击选项投票                                | 提示"投票已结束"                     | 提示正确，无法投票   | 正确     |
| 7    | 匿名投票       | 发起匿名投票，多人已投票 | 查看投票详情                                | 不显示投票人姓名，仅显示票数和百分比 | 投票人信息已隐藏     | 正确     |

根据以上测试用例，当投票主题、选项和截止时间均填写正确时，预期效果为投票创建成功并正确统计投票结果，实际项目实现效果与预期目标一致。当用户重复投票或投票已超过截止时间时，预期目标为提示相应的错误信息且投票操作被拒绝，实际项目实现效果与预期目标一致。投票决策成功界面如图 6-4 所示。

> 【图 6-4 投票决策成功界面 —— 请在此处插入截图】

### 6.4 排班管理功能测试

排班管理功能支持手动排班和 AI 智能排班。测试目的在于验证排班数据的正确存储、日历视图的准确展示以及 AI 排班功能的可用性。测试用例如表 6.4 所示。

**表 6.4 排班管理功能测试用例表**

| 编号 | 测试子项        | 预置条件               | 输入/操作                          | 预期结果                           | 执行结果             | 测试结果 |
| ---- | --------------- | ---------------------- | ---------------------------------- | ---------------------------------- | -------------------- | -------- |
| 1    | 手动指定值日    | 用户已登录，宿舍有成员 | 点击日历格子，选择成员"张三"       | 该日格子显示"张三"，背景变为绿色   | 显示正确，背景正确   | 正确     |
| 2    | 清除值日安排    | 某日已有排班           | 点击该日格子，选择"清除"           | 该日格子不再显示值日人员           | 清除成功             | 正确     |
| 3    | 保存排班        | 已手动编辑多日排班     | 点击保存按钮                       | 排班保存成功，明日值日人员收到提醒 | 保存成功，通知已发送 | 正确     |
| 4    | AI 智能排班     | 宿舍有 4 名成员        | 输入"按顺序轮流排班，周末不排"     | AI 生成排班方案，日历预览显示      | 方案合理，预览正确   | 正确     |
| 5    | AI 排班特殊需求 | 宿舍有 4 名成员        | 输入"张三周三有课不排，其他人轮流" | AI 生成方案中周三不安排张三        | 特殊需求已满足       | 正确     |
| 6    | 月份切换        | 当前月份有排班数据     | 点击前后月份切换箭头               | 正确显示对应月份的排班数据         | 切换正确，数据正确   | 正确     |

根据以上测试用例，当手动排班数据正确或 AI 排班需求描述合理时，预期效果为排班方案正确生成并展示于日历视图，实际项目实现效果与预期目标一致。AI 排班能够理解自然语言需求中的特殊约束条件并生成合理的排班方案。排班管理测试界面如图 6-5 所示。

> 【图 6-5 排班管理测试界面 —— 请在此处插入截图】

AI 智能排班结果测试界面如图 6-6 所示。

> 【图 6-6 AI 智能排班结果测试界面 —— 请在此处插入截图】

### 6.5 消息通知功能测试

消息通知功能为用户提供统一的消息管理中心。测试目的在于验证消息的正确生成、筛选搜索功能的有效性以及已读状态管理的准确性。测试用例如表 6.5 所示。

**表 6.5 消息通知功能测试用例表**

| 编号 | 测试子项     | 预置条件                   | 输入/操作           | 预期结果                                 | 执行结果               | 测试结果 |
| ---- | ------------ | -------------------------- | ------------------- | ---------------------------------------- | ---------------------- | -------- |
| 1    | 费用通知生成 | 宿舍成员 A 发起收款        | 成员 B 查看消息中心 | 收到"收款通知：电费 ¥200"消息            | 消息正确生成           | 正确     |
| 2    | 投票通知生成 | 宿舍成员 A 发起投票        | 成员 B 查看消息中心 | 收到"投票通知：聚餐地点，请参与投票"消息 | 消息正确生成           | 正确     |
| 3    | 按类型筛选   | 消息列表包含多种类型消息   | 筛选类型为"财务"    | 仅显示财务类型的消息                     | 筛选结果正确           | 正确     |
| 4    | 按状态筛选   | 消息列表包含已读和未读消息 | 筛选状态为"未读"    | 仅显示未读消息                           | 筛选结果正确           | 正确     |
| 5    | 关键词搜索   | 消息列表包含多条消息       | 搜索关键词"电费"    | 仅显示内容包含"电费"的消息               | 搜索结果正确           | 正确     |
| 6    | 点击标记已读 | 存在未读消息               | 点击一条未读消息    | 该消息标记为已读，未读角标数量减 1       | 状态更新正确，角标正确 | 正确     |
| 7    | 全部标记已读 | 存在多条未读消息           | 点击"全部已读"按钮  | 所有消息标记为已读，角标消失             | 全部已读，角标已移除   | 正确     |

根据以上测试用例，当业务模块触发通知时，预期效果为相关用户正确收到对应类型的消息，实际项目实现效果与预期目标一致。筛选、搜索和标记已读等操作均能正确执行，未读角标数量与实际未读消息数保持同步。消息中心测试界面如图 6-7 所示。

> 【图 6-7 消息中心测试界面 —— 请在此处插入截图】

---

## 7 总结与展望

### 7.1 结论

本论文围绕高校宿舍日常事务管理中的实际需求，设计并开发了一套基于 UniApp 与 Node.js 的宿舍协同管理系统。

在需求分析阶段，通过与身边同学的交流调研，梳理出宿舍生活中较为突出的若干问题：费用分摊不够透明、集体决策效率偏低、值日排班容易遗忘、公告通知触达不到位。围绕上述问题，明确了系统的功能范围和边界。

在技术方案方面，前端采用 UniApp 框架开发微信小程序，后端采用 Node.js 搭配 Express 框架提供 RESTful 接口服务，数据存储选用 MongoDB 文档型数据库。选择该技术栈的主要考量在于：前后端统一使用 JavaScript 语言有助于提升开发效率；MongoDB 的文档模型对投票选项、排班记录等结构不固定的数据具有良好的适配性。系统共实现了 11 个功能模块，覆盖宿舍管理、费用记账、投票表决、值日排班、公告通知、消息中心等应用场景。

系统的主要创新与特色体现在以下四个方面：其一，首创性地引入基于大语言模型与后置规则引擎的混合排班算法，并辅以无损合并策略与批量操作机制，大幅提升排班效率与鲁棒性；其二，构建了基于 WebSocket 的全双工实时通信架构，实现宿舍协作消息的毫秒级推送与状态同步；其三，研发了不依赖重型图表库的纯原生轻量级数据可视化大屏，赋能宿舍财务的深度分析；其四，系统实现了响应式的中、英、韩、日多语言切换与无缝夜间模式，兼顾了国际化与个性化需求。

从测试结果来看，各功能模块均能正常运行，多用户使用不同终端登录同一宿舍进行协同操作时，数据同步表现准确。整体上达到了预期的设计目标。

### 7.2 展望

尽管系统已基本实现预期功能，但在实际应用中仍存在若干不足之处，以下提出后续值得改进的方向：

（1）拓展 AI 的应用场景。当前仅在排班模块中集成了大语言模型，实际上费用分析（如月度费用对比）、事务提醒等场景同样可以尝试引入 AI 能力，但需注意控制 API 调用成本。

（2）实现多端适配。UniApp 框架本身支持编译至 H5 和 App 平台，但目前系统仅在微信小程序端进行了测试和适配。后续可考虑推出 H5 版本，以扩大系统的用户覆盖范围。

（3）加强数据安全保障。当前开发阶段接口传输尚未启用 HTTPS 加密，虽然系统采用微信登录方式不涉及用户密码存储，但若后续需要引入更多敏感信息（如手机号验证等），则需要完善传输加密和数据脱敏机制。

---

## 参考文献

[1] 陈斐, 吴林韩, 张朝阳, 等. 智慧宿舍管理系统设计与实现[J]. 信息技术与信息化, 2023, (12): 142-147.

[2] 陈柳羽, 宁雪梅, 许舟洋. 高校数字化宿舍管理系统设计与实现[J]. 电脑编程技巧与维护, 2024, (10): 113-116.

[3] 卢洋, 赵伟, 高健. 基于前后端分离架构的Web项目设计与实现[J]. 电脑编程技巧与维护, 2021, (3): 33-35.

[4] 周筱龙, 郭淼川. 基于前后端分离的供应商管理系统的升级与应用[J]. 电脑知识与技术, 2025, 21(34): 65-68.

[5] 刘亚茹, 张军. Vue.js框架在网站前端开发中的研究[J]. 电脑编程技巧与维护, 2022, (1): 18-19.

[6] 项露芬, 孙佳怡, 李梦婷. 基于Vue和Node.js的音乐门票管理系统的设计与实现[J]. 现代信息科技, 2025, 9(11): 96-101.

[7] 黄苗苗. 基于Vue和Node.js的作业管理系统的设计与实现[J]. 现代信息科技, 2024, 8(22): 102-105+110.

[8] 孟庆宇. 基于Web+MongoDB+VsFTP的城市轨道交通智能巡检感知告警系统研究[J]. 现代信息科技, 2025, 9(1): 120-126+133.

[9] 申正阳, 王枫, 任环宇. 基于JWT的EAST实验数据用户身份和服务权限认证[J]. 计算机应用与软件, 2024, 41(5): 327-331+344.

[10] 王婷, 陆克中, 黄孝, 等. 基于微信小程序的个人健康管理系统设计[J]. 信息与电脑(理论版), 2022, (10): 155-158.

[11] 胡军, 杨延云, 张宇. 基于微信小程序的高校请假系统设计与实现[J]. 现代信息科技, 2025, 9(19): 89-92+98.

[12] 刘长琦. 基于微信小程序的高校基层党组织智慧党建系统设计与实现[J]. 辽宁师专学报(自然科学版), 2025, 27(3): 44-51.

[13] 黄嬿, 江雯婷, 陆燕华, 等. 基于DeepSeek大语言模型的中药饮片处方智能点评系统应用效果评价[J]. 中国临床药学杂志, 2025, 34(9): 649-656.

[14] 王珊, 杜小勇, 陈红. 数据库系统概论[M]. 6版. 北京: 高等教育出版社, 2023.

[15] CANTELON M, HARTER M, HOLOWAYCHUK T J, et al. Node.js in Action[M]. 2nd ed. Shelter Island: Manning Publications, 2021.

[16] BRADSHAW S, BRAZIL E, CHODOROW K. MongoDB: The Definitive Guide[M]. 3rd ed. Sebastopol: O'Reilly Media, 2021.

[17] FREEMAN E. Pro Vue.js 3[M]. New York: Apress, 2022.

---

## 致谢

文末搁笔，忽觉岁短。

完成这篇论文，也就意味着大学四年的学生时代即将告一段落。在此，谨向所有给予帮助和支持的人表达最诚挚的感谢。

衷心感谢导师张丽君老师。在论文撰写和系统开发的过程中，张老师给予了悉心的指导和耐心的帮助。每次遇到困惑时，老师总是不吝时间，认真分析问题并提出建设性的意见。正是老师严谨的治学态度和对学生的关怀，这篇论文才得以顺利完成。

感谢计算机科学与技术学院为学生搭建的成长平台。感谢辅导员在学习和生活方面的关心与指导，感谢各位任课老师的用心教导，课堂上的每一堂课都为专业知识的积累奠定了坚实的基础。学院提供的学术资源和实践平台，为将理论知识应用于实际项目开发提供了宝贵的机会。

感谢同窗好友们在大学四年中的陪伴与支持。在论文写作和系统开发的日子里，相互鼓励、共同探讨，这些点滴都是前行路上的温暖动力。

最后，向家人致以最深的感恩。正是家人的理解、支持与鼓励，才能够安心求学、专注学业。

学生时代虽已落幕，未来之路正徐徐展开。将铭记这段岁月中收获的知识与情谊，带着感恩与勇气，迈向更加广阔的天地。
